import { CheckDescriptionPipe } from './check-description.pipe';

describe('CheckDescriptionPipe', () => {
  it('create an instance', () => {
    const pipe = new CheckDescriptionPipe();
    expect(pipe).toBeTruthy();
  });
});
